Also See in !A.doc    #repos.
•	API1 – X auth, X controller, X swagger, X top level
•	API2 – X auth,  X controller, swagger, top level
•	API3 – X auth,  controller, swagger, top level
•	MVC1 - X auth, top level
•	MVC2 - X auth, X top level 
--- Addl changes to NSwag studio settings
•	Uncheck the "Generate optional parameters (reorder parameters (required first, optional at the end) and generate optional parameters)"